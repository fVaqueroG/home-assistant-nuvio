name: Apply Nuvio room mappings and remote volume (one-time)
on:
  push:
    branches:
      - feature/room-mappings-volume-control
permissions:
  contents: write
concurrency:
  group: apply-nuvio-rooms
  cancel-in-progress: true
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Nuvio card patch
        run: python3 .github/scripts/patch_rooms_once.py
      - name: Syntax check
        run: node --check custom_components/nuvio/frontend/nuvio-card.js
      - name: Existing card regression tests
        run: |
          npm ci --prefix tests
          node tests/test_card.cjs
      - name: Validate patch and commit source only
        run: |
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add custom_components/nuvio/frontend/nuvio-card.js custom_components/nuvio/manifest.json
          if ! git diff --cached --quiet; then
            git commit -m 'Add per-room Nuvio playback mappings, visual editor and TV volume remote'
            git push
          fi
